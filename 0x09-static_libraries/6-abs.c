#include "main.h"
/**
 * _abs - a function that returns absolute value
 * @i: an argument of abs function
 * Return: Always 0 (Sucess)
*/
int _abs(int i)
{
	if (i >= 0)
		return (i);
	else
		return (i * -1);
}
